{% extends 'base.html.twig' %}

{% block title %}Modifier Vente{% endblock %}

{% block body %}
<h1>Modifier Vente</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-2">Modifier</button>
{{ form_end(form) }}

<a href="{{ path('app_sale_index') }}" class="btn btn-secondary mt-3">Retour</a>
{% endblock %}
